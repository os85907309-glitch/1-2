function solution(num1, num2) {
    return Math.floor(num1/num2);
    // Math.round(num1/num2);
     // Math.cell(num1/num2);
     // Math.roandom(num1/num2); 0<x<1 사이 숫자 변환
}

const ret = solution(9,5);
console.log(ret)